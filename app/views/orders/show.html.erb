<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @order.name %>
  <strong></strong>
</p>

<p>
  <strong>Address:</strong>
  <%= @order.address %>
</p>

<p>
  <strong>Email:</strong>
  <%= @order.email %>
</p>

<p>
  <strong>Pay type:</strong>
  <%= @order.pay_type %>
</p>
<% @line_items.each do|line_item|%>
<tr class="total_line">
		<td colspan="2">Total</td>
		<td class="total_cell"><%=line_item.product.title%></td></br>
		<td class="total_cell">quantity(产品数)</td>
		<td class="total_cell"><%=line_item.quantity%>件</td></br>
		<td class="total_cell">total_price(价格)</td>
		<td class="total_cell"></td><%=number_to_currency(line_item.total_price)%></br>
</tr>

<%end%>

<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
